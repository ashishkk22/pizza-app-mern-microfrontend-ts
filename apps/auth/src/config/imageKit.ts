import { environment } from '../environments/environment';

export const urlEndpoint = environment.APP_URL_ENDPOINT;
export const publicKey = environment.APP_PUBLICKEY;
export const authenticationEndpoint = environment.APP_AUTHENTICATION_ENDPOINT;
export const imagekitUploadUrl = environment.APP_IMAGEKIT_UPLOAD_URL;
